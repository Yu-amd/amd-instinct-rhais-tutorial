global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: vllm-primary
    metrics_path: /metrics
    static_configs:
      - targets: ['host.docker.internal:8000']

  # SCALE_JOBS_START
  # SCALE_JOBS_END

